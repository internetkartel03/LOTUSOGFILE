# LOTUS Status - 2026-07-03

## Checkpoint

This checkpoint narrows LOTUS to a more dependable frontend-builder milestone.

## Changes Landed

- removed the two slower local models from the active builder flow
- kept `Qwen Coder` as the single local builder path
- kept the landing page routing into the separate builder deploy
- kept live preview streaming and sanitization in place
- made the builder keep suggesting next moves after a generation finishes
- kept the chat composer available on the Preview screen
- added direct `Export HTML` from the live preview
- updated docs to match the current product reality

## Pain Points

- `src/App.tsx` is still oversized and owns too much UI behavior
- store logic and app-shell logic are split in a way that still causes duplication
- Supabase and local-first concerns still live side by side
- generated HTML quality depends heavily on prompt quality and local model stability

## Red Flags

- there is no dedicated image-generation pipeline yet, only visual generation inside HTML
- export is good enough for frontend HTML handoff, not a full app packaging story
- browser-to-local-runtime networking depends on Ollama or LM Studio being reachable from the client
- the builder still needs more battle-testing around repeated regenerate/export loops

## Best Next Handoffs

- Claude: break up `src/App.tsx`, refine continuation UX, improve system prompts
- Kimi: iterate on app-style prompt recipes and visual polish prompts
- Codex: add browser regression coverage and harden export behavior further
